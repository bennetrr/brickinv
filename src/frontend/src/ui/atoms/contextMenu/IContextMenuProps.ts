import { ReactBaseComponentProps } from '@wemogy/reactbase';
import ContextMenu from './ContextMenu';

type IContextMenuProps = ReactBaseComponentProps<typeof ContextMenu>;

export default IContextMenuProps;
