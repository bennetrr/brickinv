import React from 'react';
import IAcceptGroupInvitePageProps from './IAcceptGroupInvitePageProps';
import { observer } from 'mobx-react';

const AcceptGroupInvitePage: React.FC<IAcceptGroupInvitePageProps> = ({}) => {
  return <div>AcceptGroupInvite</div>;
};

export default observer(AcceptGroupInvitePage);
