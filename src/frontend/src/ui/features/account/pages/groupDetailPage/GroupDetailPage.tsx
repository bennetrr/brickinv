import React from 'react';
import IGroupDetailPageProps from './IGroupDetailPageProps';
import { observer } from 'mobx-react';

const GroupDetailPage: React.FC<IGroupDetailPageProps> = ({}) => {
  return <div>GroupDetails</div>
}

export default observer(GroupDetailPage);
